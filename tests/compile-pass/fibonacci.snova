package tests.run_pass.fibonacci


import builtin.console.Console


func fib(n: int): int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

func main(): int {
    Console.printline(fib(10).toString())
    return 0
}
